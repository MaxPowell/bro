// This file was automatically generated by bifcl from /home/ubuntu/bro/src/analyzer/protocol/smb/smb1_com_tree_disconnect.bif (plugin mode).


#include "smb1_com_tree_disconnect.bif.h"

EventHandlerPtr smb1_tree_disconnect; 
void BifEvent::generate_smb1_tree_disconnect(analyzer::Analyzer* analyzer, Connection* c, Val* hdr, int is_orig)
	{
	// Note that it is intentional that here we do not
	// check if ::smb1_tree_disconnect is NULL, which should happen *before*
	// BifEvent::generate_smb1_tree_disconnect is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(hdr);
	vl->append(new Val(is_orig, TYPE_BOOL));

	mgr.QueueEvent(::smb1_tree_disconnect, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
