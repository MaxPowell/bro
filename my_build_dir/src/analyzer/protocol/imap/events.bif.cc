// This file was automatically generated by bifcl from /home/ubuntu/bro/src/analyzer/protocol/imap/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr imap_capabilities; 
void BifEvent::generate_imap_capabilities(analyzer::Analyzer* analyzer, Connection* c, Val* capabilities)
	{
	// Note that it is intentional that here we do not
	// check if ::imap_capabilities is NULL, which should happen *before*
	// BifEvent::generate_imap_capabilities is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(capabilities);

	mgr.QueueEvent(::imap_capabilities, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
EventHandlerPtr imap_starttls; 
void BifEvent::generate_imap_starttls(analyzer::Analyzer* analyzer, Connection* c)
	{
	// Note that it is intentional that here we do not
	// check if ::imap_starttls is NULL, which should happen *before*
	// BifEvent::generate_imap_starttls is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());

	mgr.QueueEvent(::imap_starttls, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
