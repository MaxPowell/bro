// This file was automatically generated by bifcl from /home/ubuntu/bro/src/analyzer/protocol/gssapi/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr gssapi_neg_result; 
void BifEvent::generate_gssapi_neg_result(analyzer::Analyzer* analyzer, Connection* c, bro_uint_t state)
	{
	// Note that it is intentional that here we do not
	// check if ::gssapi_neg_result is NULL, which should happen *before*
	// BifEvent::generate_gssapi_neg_result is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(new Val(state, TYPE_COUNT));

	mgr.QueueEvent(::gssapi_neg_result, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
